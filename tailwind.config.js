/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    "./public/**/*.{html,js}", // Updated to point to the public directory
  ],
  theme: {
    extend: {},
  },
  darkMode: 'media', // Can keep or change as needed
  plugins: [],
}
